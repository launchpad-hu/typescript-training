//
// Helló, ez a TypeScript 2. ezdés!

/// FÜGGVÉNYEK

// 4 féle szintaxis - egészítsd ki!

function add(

const subtract = function (

const multiply = (a: number, b: number): number => {

const divide = (a: number, b: number): number => 

/*
Miben különböznek?

- a function statement-et (add) lehet használni a deklarációja előtt is
- a function statement és a function expression-ben (subtract) van egy láthatatlan paraméter, a this
  - az nyilacskás függvényekben (multiply, divide) lévő this a környezetben lévő this-t jelenti
- a rövidített nyilacskás függvényben (divide) egyetlen kifejezés van


*/

